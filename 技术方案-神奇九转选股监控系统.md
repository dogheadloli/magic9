# 神奇九转监控系统 — 技术方案

> 版本：v1.0（对照当前代码整理）  
> 工程：`stock-monitor`（Spring Boot 2.7.18 / Java 8 / MySQL 8）  
> 目标：对 **A 股自选池** 与 **币安现货监控池** 分别计算「神奇九转 + 组合过滤」，输出低 9 抄底 / 高 9 逃顶预警。两条业务线页面、表、接口、调度隔离，指标与信号引擎共用。

---

## 0. 现状一览

| 项 | 当前实现 |
|---|---|
| 运行形态 | 单机 Spring Boot，端口 `8964`，静态页由同一进程托管 |
| A 股范围 | 仅自选股池（`stock_pool`），沪/深/北，6 位代码 |
| A 股行情 | **腾讯财经为主**（`@Primary TencentProvider`，日 K 前复权 + 快照）；东方财富适配器保留备选 |
| A 股周期 | **仅日线**；盘中用实时价拼「当日未收盘 K 线」 |
| 加密货币 | **币安现货**，独立池 / 独立页 / `/api/crypto` |
| 加密周期 | **日 K（D1）+ 4 小时（H4）**，原始价、无复权 |
| 信号规则 | 低 9 / 高 9 必备；其余三项加分；`SCORE` 或 `STRICT` |
| 预警 | 企业微信 / 邮件 / 控制台，可插拔 |
| A 股附加能力 | 低 9 交易跟踪、回测、复盘、手工记账、东财资讯 + AI 情绪、AI 诊股 |
| 加密附加能力 | 24h 行情、双周期扫描与预警；**不做**跟踪 / 复盘 / 资讯 / 诊股 |
| 部署 | Docker 镜像 + Compose；MySQL 在宿主机 |

---

## 1. 目标与范围

### 1.1 目标

- 维护两份互不混用的监控池：A 股代码、币安现货交易对。
- 按各自日历定时拉行情、算指标、出信号、去重冷却后推送。
- Web 上看池子、K 线（含九转与信号标注）、信号列表。
- A 股额外提供买卖复盘、手工账、资讯与诊股。

### 1.2 不做

- 自动下单 / 实盘撮合。
- 港股、美股、合约、Level-2。
- 加密货币的交易跟踪、复盘页、资讯与 AI 诊股（与 A 股页面隔离）。

---

## 2. 信号定义

口径两边共用。A 股用日线；加密货币对 **日 K、4 小时** 各算一套，互不合并去重。

### 2.1 神奇九转（TD Setup）

以该周期收盘价为基准，默认连续 9 根（`strategy.td-setup-length`）：

- **低 9（抄底，`LOW_9`）**：连续满足 `收盘[i] < 收盘[i-4]`，计到 9。
- **高 9（逃顶，`HIGH_9`）**：连续满足 `收盘[i] > 收盘[i-4]`，计到 9。
- 条件打破则计数清零。

颜色只用于画图：A 股页红涨绿跌；加密页绿涨红跌。方向以枚举为准，不依赖颜色。

### 2.2 低 9 抄底（4 要素）

| 要素 | 判定（默认，均可配） |
|---|---|
| ① 低 9（必备） | 当根 TD 低 9 计数 = 9 |
| ② MACD 底背离 | 近 60 根内两个摆动低点：价格后低更低，DIF 后低更高 |
| ③ 持续缩量 | 近 5 根均量 < 前 5 根均量，且当根量 < 20 根量均 |
| ④ 回踩均线 | 最低价触及 MA20/MA60（容差 2%），收盘站上且贴近该均线 |

### 2.3 高 9 逃顶（4 要素）

| 要素 | 判定（默认，均可配） |
|---|---|
| ① 高 9（必备） | 当根 TD 高 9 计数 = 9 |
| ② MACD 顶背离 | 近 60 根内两个摆动高点：价格后高更高，DIF 后高更低 |
| ③ 放量滞涨 | 当根量 ≥ 20 根量均 × 1.8，且涨幅 ≤ 1% |
| ④ 远离均线 | BIAS20 ≥ 15% 或 BIAS60 ≥ 25% |

### 2.4 组合模式

- **SCORE（默认）**：① 必备；②③④ 各 1 分。`score >= strategy.score-threshold` 出信号；满分 3 标「强信号」。当前 yml 中 `score-threshold: 0`，即只要低 9/高 9 即可预警。
- **STRICT**：四要素全中才出。

### 2.5 低 9 交易计划（信号附带）

进场 = 当根收盘；止损 = 进场 − 1.5×ATR(14)（ATR 不可用则 −5%）；目标 = 上方 MA20（否则 +5%）。

- A 股最晚了结：信号日之后 **15 个交易日**（跳过周末/节假日）。
- 加密最晚了结：K 线日期之后 **15 个自然日**（`crypto.hold-calendar-days`）。

A 股另有实时跟踪：软止损（连续低于止损价 60 分钟）、硬止损（低于止损价 2%）、回 MA20 止盈、到期 TIME。加密货币只落计划字段，不跑跟踪表。

---

## 3. 总体架构

两条业务线共用中间计算，入口、存储、页面分开。

```
                    ┌─────────────────────────────────────┐
                    │  Web（Spring 托管 static）            │
                    │  index / analysis / crypto           │
                    │  quiet / ide（隐蔽行情板）            │
                    └───────────────┬─────────────────────┘
                                    │ REST
        ┌───────────────────────────┴───────────────────────────┐
        │                     调度层                             │
        │  ScanScheduler（A股交易时段）                           │
        │  CryptoScanScheduler（24/7）                           │
        └─────────────┬─────────────────────────┬───────────────┘
                      │                         │
        ┌─────────────▼─────────────┐ ┌─────────▼──────────────┐
        │ A 股                       │ │ 币安现货                │
        │ Tencent / EastMoney        │ │ BinanceSpotClient      │
        │ stock_pool / kline_daily   │ │ crypto_pool /          │
        │ signal_record / track      │ │ crypto_kline /         │
        │                            │ │ crypto_signal          │
        └─────────────┬─────────────┘ └─────────┬──────────────┘
                      │    PriceBar / IndicatorSeries
                      └─────────────┬──────────────┘
                                    ▼
                      IndicatorService + SignalEngine
                                    │
                                    ▼
                              AlertService
                         WECOM / EMAIL / CONSOLE
```

### 3.1 技术栈（以仓库为准）

| 层 | 选型 |
|---|---|
| 语言 / 框架 | Java 8，Spring Boot 2.7.18 |
| Web / 校验 / 邮件 | spring-boot-starter-web / validation / mail |
| ORM | Spring Data JPA，`ddl-auto: update`，MySQL 8 方言 |
| HTTP | `RestTemplate`（行情与 DeepSeek 分两个 Bean） |
| 并发限流 | 固定线程池 + Guava `RateLimiter` |
| 指标 | 自研：`Td9Calculator` / `MacdCalculator` / `DivergenceDetector` |
| 前端 | 原生 HTML/JS/CSS + [klinecharts](https://github.com/klinecharts/KLineChart)，无独立前端工程 |
| AI | DeepSeek（可选，`AI_ENABLED`） |
| 部署 | 多阶段 Dockerfile，Compose 只跑应用，MySQL 在宿主机 |

---

## 4. A 股链路

### 4.1 数据源

接口：`MarketDataProvider`（`fetchDailyKline` / `fetchQuote` / `fetchQuotes`）。

| 适配器 | 角色 | 说明 |
|---|---|---|
| `TencentProvider` | 主源 `@Primary` | `web.ifzq.gtimg.cn` 前复权日 K（`qfq`）；`qt.gtimg.cn` 快照 / 批量 |
| `EastMoneyProvider` | 备选 | `push2his` 日 K、`push2` 快照；部分网络下 K 线域名不可达 |

市场推断（`Market.inferByCode`）：

- **SH**：`6*` / `5*` / `11*` / `90*`
- **BJ**：`8*` / `4*` / `92*`
- **SZ**：其余（`0*` / `3*` 等）

成交量单位为 **手**，成交额为 **元**。回补默认 250 根。

### 4.2 自选池与日 K

- `stock_pool`：6 位数字代码，唯一；名称可从行情补全。
- `kline_daily`：`(code, trade_date)` 唯一，前复权 OHLCV。
- 盘中：`RealtimeScanService` 用当日快照覆盖/追加最后一根，再算指标。开盘前不拼实时 K，避免 0 价污染。

### 4.3 调度

`ScanScheduler` + `TradeCalendar`（周一至周五且不在 `schedule.holidays`；时段 `09:30–11:30`、`13:00–15:00`）：

| 任务 | 触发 | 行为 |
|---|---|---|
| 盘中扫描 | 每 5 分钟（`intraday-interval-ms`），仅交易时段 | 拼实时 K → 信号 → 预警；并检查未平跟踪单 |
| 盘前回补 | `0 15 9 * * MON-FRI` | 启用标的日 K 回补 |
| 收盘定稿 | `0 5 15 * * MON-FRI` | 再回补一次落当日 K；非实时检查跟踪单 |

`schedule.force-scan: true` 可忽略时段（调试）。并发 8 线程，限流 8 req/s，失败重试 3 次。

### 4.4 A 股专属模块

| 模块 | 说明 |
|---|---|
| 交易跟踪 | `signal_trade_track`，仅 `BUY_LOW9`；状态 OPEN / TP / SL / TIME |
| 回测 | 网格搜索止损 / 止盈 / 持有天数 / 分数；另有退出策略对比 |
| 复盘 | `/api/review/low9` 分页回放低 9 与跟踪结果 |
| 记账 | `manual_trade`，数量单位为 **股** |
| 资讯 | 东财检索 + 可选 DeepSeek 情绪（利好 / 利空 / 中性） |
| 诊股 | 规则分 + 可选 DeepSeek，按 `(code, trade_date)` 缓存 |

---

## 5. 币安现货链路

与 A 股完全分开：不同表、不同 Controller、不同页面、不同调度。禁止把 `BTCUSDT` 写入 `stock_pool`。

### 5.1 数据源

`BinanceSpotClient` 调公开 REST（`crypto.binance.base-url`，默认可被 `CRYPTO_BINANCE_BASE` 覆盖）：

| 用途 | 路径 |
|---|---|
| 校验现货、取 base/quote | `GET /api/v3/exchangeInfo?symbol=` |
| K 线（含未收盘最后一根） | `GET /api/v3/klines?interval=1d\|4h` |
| 24h 行情 | `GET /api/v3/ticker/24hr` |

价格为现货原始价。时间按 **UTC** 存 `open_time`。成交量为 **基础币数量**，成交额为 **计价币（多为 USDT）**。国内访问不通时把 base-url 换成 `https://data-api.binance.vision`。

交易对：规范化为大写、去掉 `-` `/` `_`，格式 `[A-Z0-9]{5,20}`，添加时校验币安现货且 `TRADING`。

### 5.2 周期

| 枚举 | 币安 interval | 回补根数 |
|---|---|---|
| `D1` | `1d` | 250 |
| `H4` | `4h` | 360 |

扫描时每个启用交易对 **两个周期都算**。信号唯一键：`(symbol, bar_interval, open_time, signal_type)`。

日常扫描只拉最近 5 根（含未收盘）upsert；库内不足约 80 根则全量回补。

### 5.3 调度

`CryptoScanScheduler`：每 5 分钟，**不看 A 股交易时段**。开关：`crypto.enabled` 与 `crypto.scan.enabled`。独立线程池（4）与限流（4 req/s）。

持有天数按自然日，不加午休软止损逻辑。

### 5.4 监控池数据

热门现货可直接写入 `crypto_pool`（如 BTC/ETH/BNB/SOL/XRP/DOGE/ADA/AVAX 的 USDT 对）。K 线由扫描或「回补 K 线」再拉，不在插入时打币安。

---

## 6. 共用计算核

### 6.1 指标

`IndicatorService`：

- A 股：`compute(code)` / `computeAsOf(code, date)`，读 `kline_daily`。
- 通用：`computeFrom(code, List<PriceBar>)`，加密货币走这条。

每根输出：TD 买卖计数与 `LOW_9`/`HIGH_9`、MACD(12/26/9)、MA5/10/20/60、量均、BIAS、ATR(14)。加密 K 线额外带 `openTime` / `openTimeMs` 供 4 小时画图。

### 6.2 信号引擎

`SignalEngine` 只看 `IndicatorSeries`，不感知市场。加密侧在 `CryptoSignalService.decorate` 里改 `assetKind=币安现货`、周期、`latestExitDate`（自然日），再写入 `crypto_signal`。

### 6.3 预警

`AlertService.dispatch(SignalResult)` 两边共用。文案用 `assetKind`、时间与周期区分「股票 / 币安现货」。  
`dispatchExit` 只服务 A 股跟踪单。

冷却：`alert.cooldown-days`（默认 1）。A 股按交易日窗口内「已通知」记录；加密按同交易对 + 同周期 + 同方向、开盘时间往前 N 日。

---

## 7. 数据模型

JPA 启动时 `ddl-auto: update` 建表/补列。库名默认 `stock`。

### 7.1 A 股

```
stock_pool          uk(code)
kline_daily         uk(code, trade_date)
signal_record       uk(code, trade_date, signal_type)
signal_trade_track  uk(signal_id)          -- 低9生命周期
manual_trade                               -- 手工多头记账
news_item                                  -- 资讯 + 情绪
stock_diagnosis     idx(code, trade_date)  -- AI 诊股缓存
```

### 7.2 加密货币

```
crypto_pool         uk(symbol)
crypto_kline        uk(symbol, bar_interval, open_time)
crypto_signal       uk(symbol, bar_interval, open_time, signal_type)
```

`bar_interval` 存枚举名 `D1` / `H4`。价格精度按 8 位小数设计，以兼容山寨币。

---

## 8. Web 页面

全部在 `src/main/resources/static/`，浏览器直开，无单独前端服务。

| 页面 | 用途 |
|---|---|
| `index.html` | A 股控制台：自选池、日 K、诊股、舆情、信号；交易时段每 60s 刷新 |
| `analysis.html` | 低 9 复盘 + 手工记账 |
| `crypto.html` | 币安现货：监控池、日 K/4 小时切换、24h 行情、当前信号、信号列表；扫描开启时每 60s 刷新 |
| `quiet.html` / `ide.html` | 隐蔽行情板（A 股报价），不混加密 |

页面互链：股票控制台 ↔ 币安现货 ↔ 买卖分析。

画图：klinecharts，叠加低 9 / 高 9 / 抄底 / 逃顶标注。

---

## 9. REST 接口

### 9.1 A 股

| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST | `/api/pool` | 自选列表 / 新增 `{code,name?,group?}` |
| DELETE | `/api/pool/{id}` | 删除 |
| PUT | `/api/pool/{id}/enabled?enabled=` | 启停 |
| POST | `/api/kline/backfill?code=` | 回补日 K |
| GET | `/api/kline?code=` | 已落库日 K |
| GET | `/api/quote?code=` | 单只快照 |
| GET | `/api/quotes?codes=` | 批量快照 |
| GET | `/api/indicator?code=&limit=` | 已落库指标 |
| GET | `/api/indicator/realtime?code=` | 含当日未收盘 K |
| GET | `/api/signal/evaluate?code=&asOf=` | 评估（不落库） |
| POST | `/api/signal/scan` | 按库内日 K 扫描 |
| POST | `/api/signal/scan-realtime` | 盘中扫描 + 推送 |
| GET | `/api/signals?code=&date=&type=` | 信号查询 |
| GET | `/api/market/status` | 是否交易日/时段 |
| POST | `/api/alert/test?type=` | 试推预警 |
| POST/GET | `/api/backtest` | 回测 |
| GET | `/api/backtest/exit-comparison` | 退出策略对比 |
| GET | `/api/review/low9?page=&size=` | 低 9 复盘 |
| GET | `/api/analysis/{code}/current` | 当前决策 |
| GET | `/api/analysis/{code}/history` | 历史模拟 |
| GET/POST | `/api/journal` | 记账列表 / 建仓 |
| PUT | `/api/journal/{id}/close` | 平仓 |
| DELETE | `/api/journal/{id}` | 删除账本 |
| GET | `/api/news/{code}` | 资讯 |
| GET | `/api/diagnosis/{code}` | 诊股 |
| POST | `/api/trade-tracks/sync` `/rebuild` `/check` | 跟踪维护 |

### 9.2 币安现货（`/api/crypto`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/crypto/status` | 开关、交易所、周期 |
| GET/POST | `/api/crypto/pool` | 监控池；新增 `{symbol,name?,group?}` |
| DELETE | `/api/crypto/pool/{id}` | 删除 |
| PUT | `/api/crypto/pool/{id}/enabled?enabled=` | 启停 |
| POST | `/api/crypto/kline/backfill?symbol=&interval=` | 回补；interval 空则 D1+H4 |
| GET | `/api/crypto/kline?symbol=&interval=` | 已落库 K 线 |
| GET | `/api/crypto/quote?symbol=` | 24h ticker |
| GET | `/api/crypto/indicator?symbol=&interval=&live=` | 指标；`live=true` 先拉最新 K |
| GET | `/api/crypto/signal/evaluate?symbol=&interval=` | 评估（不落库） |
| POST | `/api/crypto/scan` | 全池双周期扫描 |
| GET | `/api/crypto/signals?symbol=&interval=&type=` | 信号查询 |

---

## 10. 配置要点

文件：`src/main/resources/application.yml`。密钥只走环境变量（见 `.env.example`）。

| 段 | 作用 |
|---|---|
| `server.port` | 默认 8964 |
| `spring.datasource` / `jpa` | MySQL `stock`，上海时区，`ddl-auto: update` |
| `spring.mail` | 邮件预警 SMTP |
| `market.*` | 腾讯 / 东财 URL，回补 250 根 |
| `strategy.*` | TD9 / MACD / 背离 / 量能 / 均线 / 打分 / 软硬止损 |
| `schedule.*` | A 股时段、节假日、盘前/收盘 cron、线程与限流 |
| `crypto.*` | 总开关、币安 URL、日 K/4H 根数、24/7 扫描 |
| `alert.*` | 渠道、企微 key、冷却天数、邮件收发件 |
| `ai.deepseek.*` | 诊股 / 情绪（可选） |

常用环境变量：`DB_URL` / `DB_USER` / `DB_PASS`、`WECOM_KEY`、`MAIL_*`、`CRYPTO_ENABLED`、`CRYPTO_BINANCE_BASE`、`AI_ENABLED`、`DEEPSEEK_API_KEY`。

---

## 11. 工程结构

```
com.stock
├─ StockMonitorApplication          @EnableScheduling
├─ config/                          策略、调度、预警、AI、RestTemplate、扫描线程池
├─ datafetch/                       MarketDataProvider、Tencent、EastMoney
├─ domain/ + repository/            A 股实体与仓库
├─ service/                         StockPoolService、KlineService
├─ indicator/                       PriceBar、指标序列、TD9/MACD/背离
├─ signal/                          SignalEngine、SignalService、类型与要素
├─ scan/                            TradeCalendar、RealtimeScan、ScanScheduler
├─ tracking/                        低 9 跟踪
├─ backtest/ analysis/ journal/
├─ news/ diagnosis/ ai/
├─ alert/                           AlertService + 渠道
├─ crypto/                          币安客户端、池/K线/信号、扫描、CryptoController
└─ web/                             A 股 REST
src/main/resources/static/          index / analysis / crypto / quiet / ide
```

---

## 12. 调度与并发对照

| | A 股 | 币安现货 |
|---|---|---|
| 何时扫 | 交易日 + 配置时段 | 全天（开关打开即扫） |
| 间隔 | 5 分钟 | 5 分钟 |
| 周期 | 日 K（盘中拼未收盘） | 日 K + 4 小时 |
| 日切 | 工作日 09:15 回补、15:05 定稿 | 无 A 股式收盘任务；扫描时 upsert 最新 K |
| 线程 / 限流 | 8 / 8 rps | 4 / 4 rps |
| 持有到期 | 15 个交易日 | 15 个自然日 |

---

## 13. 预警

- 渠道：`WECOM`、`CONSOLE`、`EMAIL`（未配 key/SMTP 的渠道自动跳过）。
- 同向去重：表唯一键；冷却见 §6.3。
- 企微 markdown：买入 info、卖出 warning；加密消息带「币安现货」和周期。
- 试推：`POST /api/alert/test`。

---

## 14. 部署

```
开发机 / IDE：mvn spring-boot:run → http://localhost:8964
Docker：docker build → compose up（映射 8964，DB 走 host.docker.internal）
CI：git push → Jenkins 构建推镜像 → SSH 主机 docker compose pull && up -d
```

| 文件 | 作用 |
|---|---|
| `Dockerfile` | 多阶段 Maven + Temurin 8 JRE |
| `docker-compose.yml` | 只跑应用 |
| `Jenkinsfile` | 构建 / 推送 / 部署 |
| `.env.example` | 环境变量模板（勿提交真实 `.env`） |
| `deploy/README.md` | 服务器与 Jenkins 说明 |

时区：`Asia/Shanghai`（`JAVA_OPTS` / JDBC `serverTimezone`）。币安 K 线时间戳按 UTC 存储与绘图。

---

## 15. 非功能

- **规模**：按自选 / 现货监控池设计，不是全市场扫描。
- **容错**：单标的失败不影响整轮；扫描重试；行情 0 价不拼进 K 线。
- **扩展**：数据源、通知渠道、指标输入（`PriceBar`）已接口化。
- **合规**：行情与信号仅供研究，不构成投资建议；注意免费接口与币安权重，避免封禁。

---

## 16. 两边差异速查

| | A 股 | 币安现货 |
|---|---|---|
| 代码 | 6 位数字 | `BTCUSDT` 等 |
| 复权 | 前复权 | 无 |
| 量 / 额 | 手 / 元 | 币数量 / USDT |
| K 线键 | `trade_date` | `open_time` + 周期 |
| 图色 | 红涨绿跌 | 绿涨红跌 |
| 跟踪 / 复盘 / 资讯 / 诊股 | 有 | 无 |
| 页面 | `index.html`、`analysis.html` | `crypto.html` |
| 接口前缀 | `/api/pool` 等 | `/api/crypto` |
