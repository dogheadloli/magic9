# 神奇九转批量选股监控系统 — 技术方案文档

> 版本：v0.3（关键项已确认）
> 语言/技术栈：Java + Spring Boot
> 目标：维护一批股票代码，定时批量拉取行情，基于"神奇九转 + 组合过滤条件"输出**低9抄底**与**高9逃顶**预警信号。

## 0. 已确认决策（v0.2）

| 决策项 | 结论 |
|---|---|
| 行情数据源 | **东方财富免费接口**（无 token，日K前复权 + 盘中快照），适配器可后续切换 |
| 监控范围 | **仅自选股池**（用户维护代码列表，规模小，数据源压力低） |
| 运行节奏 | **盘中周期实时监控**（交易时段内每 N 分钟扫描一次） |
| K 线周期 | **仅日线**；盘中用最新价作为"当日未收盘 K 线"实时参与计算（详见 §7.1） |
| 预警渠道 | **企业微信机器人**（Webhook） |
| 组合判定 | **加权打分模式**（低9/高9 必备 + 其余每项 1 分，≥2 分预警、满 3 分标"强信号"） |
| 管理界面 | **需要 Web 页面**：维护自选股 + 查看 K 线图（含信号标注）+ 查看信号列表（详见 §3.3） |
| 均线口径 | **MA20 / MA60**（不含 MA120） |
| 部署形态 | **单机 + MySQL 持久化** |

---

## 1. 目标与范围

### 1.1 核心目标
- 维护一份可增删改的**股票池**（自选股 / 全市场）。
- 定时（盘后或盘中按周期）**批量**拉取个股 K 线与行情数据。
- 计算 **神奇九转（TD 九转序列）** 计数，并叠加组合条件，识别两类信号：
  - **低 9 抄底组合**：低9（红色9）+ MACD 底背离 + 成交量持续缩量 + K 线踩均线支撑
  - **高 9 逃顶组合**：高9（绿色9）+ MACD 顶背离 + 放量滞涨 + 股价远离均线
- 命中后通过通知渠道（钉钉/企业微信/邮件/控制台）输出**预警信号**，并落库留痕。

### 1.2 非目标（本期不做）
- 不做自动下单 / 实盘交易。
- 不做 Level-2、逐笔成交、龙虎榜等深度数据分析（可作为后续扩展）。
- 不做策略历史回测引擎（可作为 Phase 2 扩展）。

---

## 2. 信号定义（核心算法规格）

> 这是整个系统最关键的部分。先统一口径，便于后续编码与你校对。

### 2.1 神奇九转 / TD 九转序列计数规则

以**日线收盘价**为基准（周期可配置）：

- **低9（抄底 setup，本文档标记为"红色9"）**
  连续满足：`收盘价[i] < 收盘价[i-4]`，连续计数 1→9。当第 9 根 K 线满足时，记为 **低9 完成信号**（潜在底部）。
- **高9（逃顶 setup，本文档标记为"绿色9"）**
  连续满足：`收盘价[i] > 收盘价[i-4]`，连续计数 1→9。当第 9 根 K 线满足时，记为 **高9 完成信号**（潜在顶部）。
- 任意一根 K 线打破条件则计数清零、重新开始。

> ⚠️ **颜色约定需确认**：你给的描述是"低9抄底=红色9、高9逃顶=绿色9"（按 A 股红涨绿跌的情绪色）。
> 而**东方财富等平台的"神奇九转"默认相反**：连续 9 天收盘价高于前 4 天显示在 K 线**上方**（顶部/卖出），连续 9 天低于前 4 天显示在**下方**（底部/买入）。
> 颜色只是展示标签，**真正决定信号方向的是上面的计数逻辑**。本方案以"低9=抄底、高9=逃顶"为准（与你的业务意图一致），代码内用枚举 `LOW_9 / HIGH_9` 表示，不依赖颜色字段，避免歧义。

### 2.2 低 9 抄底组合（4 要素）

| 要素 | 含义 | 可量化判定（默认参数，均可配置） |
|---|---|---|
| ① 低9 信号 | TD 低9 计数完成 | 当根 K 线 TD 低9 计数 == 9 |
| ② MACD 底背离 | 价格创新低，MACD 不创新低 | 最近两个价格低点：`价格后低点 < 前低点` 且 `DIF(或MACD柱)后低点 > 前低点` |
| ③ 成交量持续缩量 | 量能逐步萎缩 | 近 N 日（默认 5）成交量均量 < 前 N 日均量；或连续 ≥3 日量能递减且低于 20 日均量 |
| ④ K 线踩均线支撑 | 回踩关键均线获支撑 | 当日最低价触及目标均线（默认 MA20/MA60，可配），且收盘收于该均线之上，`|收盘-均线|/均线 ≤ 阈值(默认2%)` |

### 2.3 高 9 逃顶组合（4 要素）

| 要素 | 含义 | 可量化判定（默认参数，均可配置） |
|---|---|---|
| ① 高9 信号 | TD 高9 计数完成 | 当根 K 线 TD 高9 计数 == 9 |
| ② MACD 顶背离 | 价格创新高，MACD 不创新高 | 最近两个价格高点：`价格后高点 > 前高点` 且 `DIF(或MACD柱)后高点 < 前高点` |
| ③ 放量滞涨 | 成交量放大但价格不涨 | 成交量 ≥ 20 日均量 × 倍数(默认1.5~2)，且当日涨幅 ≤ 阈值(默认1%) 或收阴/带长上影 |
| ④ 股价远离均线 | 乖离过大 | 乖离率 `BIAS = (收盘-MA)/MA×100`，目标均线（默认 MA20/MA60）BIAS ≥ 阈值（默认 MA20 +15% 或 MA60 +25%，按品种波动调整） |

### 2.4 组合命中模式（可配置）

- **严格模式**：4 要素全部满足才出信号（信号少、质量高）。
- **加权打分模式**（默认推荐）：①低9/高9 为**必要项**，②③④为**加分项**，每项给权重，总分 ≥ 阈值即预警，并在通知里给出"命中要素清单 + 评分"。
  - 例如：低9(必备) + 其余每项 1 分，≥2 分预警，满分 3 分标记"强信号"。

---

## 3. 系统架构

### 3.1 分层架构

```
┌──────────────────────────────────────────────────────────┐
│  调度层 Scheduler (Spring Scheduling / Quartz)             │
│   - 盘后全量任务 / 盘中周期任务 / 股票池同步任务            │
└───────────────┬──────────────────────────────────────────┘
                │
┌───────────────▼──────────────┐   ┌───────────────────────┐
│ 数据采集层 DataFetch          │   │ 股票池管理 StockPool   │
│  - 行情源适配器(可插拔)        │   │  - 增删改查 / 分组      │
│  - K线/快照拉取 + 限流 + 重试  │   │  - 全市场/自选         │
└───────────────┬──────────────┘   └───────────────────────┘
                │ OHLCV(前复权)
┌───────────────▼──────────────┐
│ 指标计算层 Indicator          │
│  - TD9 / MACD / MA / 量能 /    │
│    BIAS / 背离检测            │
└───────────────┬──────────────┘
                │
┌───────────────▼──────────────┐
│ 信号判断层 SignalEngine       │
│  - 低9抄底组合 / 高9逃顶组合   │
│  - 打分 / 去重 / 冷却         │
└───────────────┬──────────────┘
                │
┌───────────────▼──────────────┐   ┌───────────────────────┐
│ 预警输出层 Alert              │   │ 存储层 Persistence     │
│  - 企业微信/控制台/落库        │   │  - 股票池/K线/信号记录 │
└──────────────────────────────┘   └───────────┬───────────┘
                                                │
┌───────────────────────────────────────────────▼──────────┐
│ Web 管理与可视化层 (REST API + 前端)                       │
│  - 自选股增删改查  - K线图(含信号标注)  - 信号列表查询      │
└──────────────────────────────────────────────────────────┘
```

### 3.2 技术选型（建议）

| 模块 | 选型 | 说明 |
|---|---|---|
| 框架 | Spring Boot 3.x (JDK 17) | 主流、生态完善 |
| 定时调度 | Spring `@Scheduled` / Quartz | 单机用 `@Scheduled` 即可；需分布式/可视化用 Quartz 或 XXL-Job |
| HTTP 客户端 | OkHttp / Spring WebClient | 拉行情，支持连接池、超时、重试 |
| JSON | Jackson | Spring 自带 |
| 技术指标 | 自研 + 可选 [ta4j](https://github.com/ta4j/ta4j) | MA/MACD 可用 ta4j；TD9、背离需自研 |
| 持久化 | MyBatis-Plus / Spring Data JPA | 二选一 |
| 数据库 | MySQL 8（生产）/ H2（本地） | K线历史可选 SQLite/文件缓存 |
| 并发 | `ThreadPoolExecutor` + 限流(Guava RateLimiter / Resilience4j) | 批量拉取控速防封 |
| 通知 | 企业微信机器人 Webhook（markdown） | 渠道可插拔，便于以后加邮件/钉钉 |
| 配置 | `application.yml` + DB 配置表 | 阈值热更新 |
| Web 后端 | Spring MVC REST | 提供股票池/K线/信号 JSON 接口 |
| Web 前端 | Vue3 + Vite + [klinecharts](https://github.com/klinecharts/KLineChart)（专业K线库，原生支持标注） | 单机可由 Spring Boot 托管静态资源；轻量也可用 Thymeleaf + ECharts |

### 3.3 Web 管理与可视化模块（已确认：需要）

**页面功能**
1. **自选股管理页**：表格展示股票池，支持新增/删除/启停/分组；新增时按代码自动补全名称（调东方财富）。
2. **K 线图页**：选中某只股票，展示日 K 线（蜡烛图 + 成交量 + MA20/MA60 + MACD 副图），并在 K 线上**叠加标注**：
   - 低9（红色9）/ 高9（绿色9）计数点；
   - 命中的抄底/逃顶信号点（带 tooltip：命中要素 + 评分）。
3. **信号列表页**：按日期/方向/股票筛选历史信号，展示评分、命中要素、现场快照，支持导出。

**后端 REST 接口（草案）**
| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST/DELETE | `/api/pool` | 自选股增删查 |
| GET | `/api/kline?code=&limit=` | 返回日K + MA/MACD + TD计数（供前端画图标注） |
| GET | `/api/signals?date=&type=&code=` | 信号查询 |
| GET | `/api/stock/search?kw=` | 代码/名称补全 |

**说明**：单机部署，前端构建产物放入 `src/main/resources/static`，由 Spring Boot 一并托管，浏览器直接访问，无需单独前端服务器。

---

## 4. 数据源方案（已确认：东方财富免费接口）

采用**东方财富免费 HTTP 接口**，Java 端直接调用，封装为 `MarketDataProvider` 接口 + `EastMoneyProvider` 适配器，后续可无痛切换 Tushare/付费源。

| 用途 | 接口 | 说明 |
|---|---|---|
| 日K历史（前复权） | `push2his.eastmoney.com/api/qt/stock/kline/get` | `klt=101`(日) `fqt=1`(前复权)，回补近 250 根 |
| 盘中实时快照 | `push2.eastmoney.com/api/qt/stock/get`（或 `ulist` 批量） | 取最新价/今开/最高/最低/成交量，构造"当日未收盘 K 线" |

- `secid` 拼接规则：沪市 `1.600519`，深市 `0.000001`，按代码段映射 SH/SZ/BJ。
- 由于仅监控自选股（规模小），数据源压力低；仍保留限流(默认 8 req/s)与指数退避重试(3 次)防偶发限制。
- 历史日K盘前/启动时回补并落库，盘中只增量拉快照，降低请求量。

> 备选源（已封装接口，按需切换）：腾讯 ifzq.gtimg.cn、Tushare Pro(token)。免费源仅供研究使用。

---

## 5. 数据模型（表设计草案）

```sql
-- 股票池
stock_pool(
  id, code, name, market,          -- market: SH/SZ/BJ
  group_name,                      -- 分组(自选/行业/策略)
  enabled, create_time, update_time
)

-- 日K线（前复权）
kline_daily(
  id, code, trade_date,
  open, high, low, close, pre_close,
  volume, amount, change_pct,
  adjust_flag,                     -- 复权类型
  UNIQUE(code, trade_date)
)

-- 指标快照（可选，缓存当日计算结果，便于复盘）
indicator_snapshot(
  id, code, trade_date,
  td_count, td_type,               -- LOW_9/HIGH_9/NONE
  dif, dea, macd,
  ma5, ma10, ma20, ma60,        -- 已确认仅用 MA20/MA60(MA5/10留作展示)
  vol_ma5, vol_ma20,
  bias20, bias60
)

-- 信号记录
signal_record(
  id, code, name, trade_date,
  signal_type,                     -- BUY_LOW9 / SELL_HIGH9
  score, max_score,
  hit_factors,                     -- JSON: 命中要素明细
  detail_json,                     -- 现场数据快照
  notified, notify_channel, create_time,
  UNIQUE(code, trade_date, signal_type)   -- 同日同向去重
)
```

---

## 6. 关键算法伪代码

### 6.1 TD 九转计数

```text
function computeTd(closes[]):
  buySetup = 0   // 低9
  sellSetup = 0  // 高9
  result[] // 每根K线的计数与类型
  for i in 4..n-1:
     if close[i] < close[i-4]: buySetup++; sellSetup=0
     else if close[i] > close[i-4]: sellSetup++; buySetup=0
     else: buySetup=0; sellSetup=0
     if buySetup==9: result[i]=LOW_9
     elif sellSetup==9: result[i]=HIGH_9
     // 计数到9后通常重置（可配是否延续计 10/11/12/13）
  return result
```

### 6.2 MACD 背离检测（思路）

```text
1. 计算 DIF/DEA/MACD柱 (默认 12,26,9)
2. 在最近 window(默认60根) 内识别价格的"摆动低点/高点"
   (局部极值: 比左右各 k 根都低/高, k 默认2)
3. 取最近两个低点 P1<P2(时间序), 对应 DIF 值 D1,D2:
   底背离 = price2 < price1 且 D2 > D1
   顶背离 = (取两个高点) price2 > price1 且 D2 < D1
```

### 6.3 信号引擎（低9抄底为例）

```text
factors = []
if td[today]==LOW_9: factors.add(TD_LOW9)        // 必备
if macdBullishDivergence(): factors.add(DIVERGENCE)
if volumeShrinking(): factors.add(VOL_SHRINK)
if touchMaSupport(): factors.add(MA_SUPPORT)

if factors.contains(TD_LOW9):
    score = factors.size - 1            // 除必备外的加分
    if score >= threshold: emitSignal(BUY_LOW9, factors, score)
```

---

## 7. 调度与并发（已确认：盘中周期实时）

### 7.1 盘中实时监控口径
- 启动 / 每日盘前：回补自选股**日K历史前复权**（默认近 250 根，满足 TD/MACD/MA60 及背离窗口计算）并落库。
- **交易时段内每 N 分钟（默认 5 分钟，可配）**触发一次扫描：
  1. 批量拉取自选股**实时快照**（最新价、今开/最高/最低、累计成交量）。
  2. 用快照构造**当日"未收盘"日 K 线**，拼接到历史日K序列末尾。
  3. 重新计算 TD9 / MACD / MA / 量能 / BIAS / 背离，跑信号引擎。
  4. 命中则按"去重 + 冷却"推送企业微信。
- 交易日历 + 交易时段校验：仅 09:30–11:30、13:00–15:00 的交易日运行；非交易时段休眠。
- 收盘后（15:05）做一次**当日 K 线定稿**：用收盘数据覆盖盘中临时K线并落库，作为次日历史。
- **周期已确认：仅日线**（最贴合"神奇九转抄底/逃顶"原意），暂不做多周期。

### 7.2 并发与限流
- 自选股规模小，固定线程池（默认 8），Guava `RateLimiter` 默认 8 req/s；失败指数退避重试 3 次。
- 盘中只增量拉快照，单标的失败不影响整体，记录失败列表下轮重试。

---

## 8. 预警输出（已确认：企业微信机器人）

- 主渠道：**企业微信群机器人 Webhook**（`https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx`），消息类型用 `markdown` 以支持加粗/颜色/换行。
- 架构上仍做成可插拔 `AlertChannel`，便于以后追加邮件/钉钉；控制台日志 + 落库始终保留。
- **去重与冷却**：盘中多次扫描时，同一标的同方向**当日只推一次**（命中 `signal_record` 唯一键即跳过）；可配 N 日内同向冷却。
- 企业微信需配置：机器人 `Webhook Key`（敏感，放配置/环境变量，不入库不入仓库）。
- 消息模板示例：

```
【低9抄底预警 · 强信号 3/3】
贵州茅台(600519)  2026-06-24
✓ 低9完成  ✓ MACD底背离  ✓ 持续缩量  ✓ 回踩MA60支撑
收盘 1580.0  量比 0.6  距MA60 +0.8%
```

---

## 9. 可配置项（application.yml 草案）

```yaml
strategy:
  kline-period: DAILY          # DAILY/WEEKLY
  td:
    setup-length: 9
  macd: {fast: 12, slow: 26, signal: 9}
  ma-support: [20, 60]
  ma-support-tolerance: 0.02
  vol-shrink-days: 5
  vol-shrink-ma: 20
  vol-surge-ratio: 1.8
  surge-stall-change: 0.01
  bias:
    ma20-sell: 15
    ma60-sell: 25
  combo-mode: SCORE            # 已确认：加权打分
  score-threshold: 2
fetch:
  provider: EASTMONEY          # 已确认：东方财富
  history-bars: 250
  thread-pool-size: 8
  rate-per-second: 8
  retry: 3
alert:
  channels: [WECOM, CONSOLE]   # 已确认：企业微信 + 控制台
  wecom-webhook-key: ${WECOM_KEY}   # 放环境变量，勿入库/入仓
  cooldown-days: 1
schedule:
  mode: INTRADAY               # 已确认：盘中周期实时
  intraday-interval-minutes: 5
  trade-sessions: ["09:30-11:30", "13:00-15:00"]
  prefetch-history-cron: "0 15 9 * * MON-FRI"   # 盘前回补历史
  eod-finalize-cron: "0 5 15 * * MON-FRI"        # 收盘定稿当日K线
datasource:                    # 已确认：单机 MySQL
  url: jdbc:mysql://localhost:3306/stock?useSSL=false&serverTimezone=Asia/Shanghai
```

---

## 10. 工程结构（建议）

```
stock-monitor/
├─ pom.xml
└─ src/main/java/com/xxx/stock
   ├─ StockMonitorApplication.java
   ├─ config/            # 配置/线程池/限流
   ├─ pool/              # 股票池管理
   ├─ datafetch/
   │   ├─ MarketDataProvider.java        # 接口
   │   └─ provider/EastMoneyProvider.java
   ├─ domain/            # Kline, StockInfo, IndicatorResult, Signal
   ├─ indicator/         # Td9Calculator, MacdCalculator, MaCalculator,
   │                     #   DivergenceDetector, VolumeAnalyzer, BiasCalculator
   ├─ signal/            # SignalEngine, Low9Rule, High9Rule
   ├─ alert/             # AlertChannel, DingTalkChannel, EmailChannel
   ├─ scheduler/         # ScanJob, TradeCalendar
   ├─ repository/        # MyBatis/JPA
   └─ web/               # PoolController, KlineController, SignalController
src/main/resources/static/   # 前端构建产物(Vue3 + klinecharts)，Spring Boot 托管
frontend/                    # 前端工程源码(Vue3 + Vite)，构建后拷贝到 static
```

---

## 11. 非功能性

- **性能**：全市场约 5000 标的，盘后全量在限流下约数分钟级；指标计算 O(n) 轻量。
- **容错**：单标的失败不影响整体；记录失败列表并重试；数据缺口告警。
- **可扩展**：策略规则、数据源、通知渠道均接口化；后续可加回测、Web 看板。
- **合规/风险**：免费数据源仅供研究；信号为辅助决策，非投资建议；注意接口频率，避免被封 IP。

---

## 12. 开发里程碑（建议）

1. **M1 脚手架 + 数据采集**：Spring Boot + MySQL 工程、东方财富适配器、日K落库、自选股池 CRUD（REST）。
2. **M2 指标与信号**：TD9 / MACD / MA20/MA60 / 量能 / BIAS / 背离 + 信号引擎（低9抄底 + 高9逃顶，加权打分）。
3. **M3 盘中调度 + 预警**：交易时段每5分钟扫描、实时K线拼接、去重冷却、企业微信推送。
4. **M4 Web 页面**：自选股管理页 + K线图(含信号标注) + 信号列表页（Vue3 + klinecharts，Spring 托管）。
5. **M5（可选）**：配置热更新、回测、更多数据源/通知渠道。

---

## 13. 确认状态

✅ **已全部确认**：
- 数据源：东方财富免费接口
- 标的范围：自选股池
- 运行节奏：盘中每 5 分钟实时扫描
- K 线周期：仅日线
- 通知渠道：企业微信机器人
- 判定模式：加权打分（低9/高9 必备 + 其余每项 1 分，≥2 分预警、满 3 分强信号）
- 均线口径：MA20 / MA60
- 管理界面：Web 页面（自选股管理 + K线图含信号标注 + 信号列表）
- 部署：单机 + MySQL
- 颜色口径：低9=抄底、高9=逃顶（代码用枚举 `LOW_9/HIGH_9`，不依赖平台颜色）

仅剩 1 项联调时再补即可：
- **企业微信机器人 Webhook Key**：可先用环境变量 `WECOM_KEY` 占位，联调时填入。

> 方案已可落地。下一步开始 **M1：工程脚手架 + 东方财富适配器 + 自选股池 + 日K落库**。
